# CLAUDE.md — cheder-maale-amos (מעקב תלמידים)

## ⚠️ DEPLOY — קרא לפני כל push!
**אתר הפרודקשן היחיד:** https://9309249-ctrl.github.io/cheder-maale-amos/
- remote: **`ctrl`** = `github.com/9309249-ctrl/cheder-maale-amos` (branch `main`).
- **deploy = `git push ctrl HEAD:main`** → GitHub Pages בונה אוטומטית (SW network-first, רענון מספיק).

**אל תדחוף ל-`orgnew` (maale-amos/cheder)** — זה **אתר מת** (DEMO, SUPABASE ריק, Pages כובה 2026-08-14).
בעבר נדחפו שינויים לשם בטעות והם לא הופיעו לעמנואל. `origin` (yossi6742853) = fork אישי מפוצל.

## Supabase
- project ref `jpoxcwtigyvzjrlejzri`. anon key ב-`js/config.js` (ציבורי; הגנה=RLS).
- כתיבה/DDL/מיגרציות: Management API PAT ב-`_CREDENTIALS.md` (gitignored) —
  `POST https://api.supabase.com/v1/projects/jpoxcwtigyvzjrlejzri/database/query`.

## טבלת students — שדות (כולם קיימים ב-DB ובטופס `js/students.js`)
name, family, tz, class_id, birthdate, birthdate_heb, parent_name, parent_phone,
**mother_name, mother_phone, mother_email**, address, status, notes.
- הטופס "תלמיד חדש"/עריכה מציג את **כל** השדות (יושר לעמודות האקסל של עמנואל, 2026-08-14).
- לחיצה על שם תלמיד ברשימה פותחת את כרטיס התלמיד.

## מודל הרשאות (בקשת עמנואל, 2026-08-17) — אל תשנה בלי אישור
- **כל צוות רואה את כל התלמידים** (שמות+פרטים מלאים).
- **כל צוות מדווח על כל תלמיד** (INSERT פתוח בטבלאות הדיווח).
- **צפייה בנתונים/דיווחים — רק לכיתה המשויכת** (SELECT class-scoped ב-RLS; + רואים תמיד את מה שאתה עצמך הזנת).
- מיגרציה אוטוריטטיבית: **`supabase/migration_report_on_all_view_own_class.sql`** (רצה ב-DB).
- ⚠️ **אל תריץ** `migration_report_visibility.sql` / `migration_report_scoping*.sql` — הן יישום ישן ומגביל ("דיווח רק על כיתתך") ש**יחזיר את הבאג** שעמנואל התלונן עליו.
- צד-לקוח: `getStudents`/`accessibleIds` ב-`js/students.js` לא מסננים כיתה (RLS אוכף בשרת).

## התחברות
לפי **מספר טלפון** (לא שם). מייל סינתטי `{טלפון}@bht.co.il`. כניסה-לפי-שם מושבתת בכוונה (אבטחה — אל תחזיר grant ל-anon).
מנהל: עמנואל רקובסקי, טלפון 0548451402.

## שינויים 2026-08-19 (בקשות עמנואל במייל)
- **סבסוד הזנה — מחיקת בקשה:** כפתור פח בטבלה ובחלון הפרטים (`takanot-admin.html` + `TakanotSB.remove`).
  מנהל בלבד (RLS `takanot_admin_delete`); מוחק גם את המסמכים ב-Storage. השורה נמחקת קודם ורק אז הקבצים.
- **משתמש מושבת:** ה"מחיקה" במסך ההרשאות היא השבתה (`active=false`). עכשיו המושבתים נשארים ברשימה
  עם תגית "מושבת" וכפתור **החזרה לפעילות**, כניסה שלהם נחסמת עם הודעה מפורשת, והוספה מחדש של אותו טלפון
  משחזרת את הפרופיל הקיים במקום ליפול על "המספר כבר קיים".
  ⚠️ `is_staff()` דורש `active` — משתמש מושבת לא רואה **שום** נתון (זה מה שקרה למחנך של כיתה א', 19/08).
- **מעקב קריאה:** הציון נכתב חופשי **1–100** (היה בחירה 1–10; ציונים ישנים נשמרים כמות שהם),
  ועריכת שמות העמודות/הוספה/סדר זמינה גם ב**הגדרות והרשאות** (כרטיס "עמודות מעקב קריאה").

## שינוי 2026-08-20 (בקשת עמנואל במייל)
- **מעקב קריאה — שורת "ממוצע כיתתי":** בתחתית טבלת הכיתה (`js/reading-assess.js`, `drawGrid`) נוספה `tfoot`
  עם ממוצע אוטומטי לכל עמודה + מספר התלמידים שנספרו בסוגריים.
  - נספרים **רק** תלמידים שיש להם ציון בעמודה — מי שאין לו ציון אינו נחשב 0.
  - `null`/מחרוזת ריקה מסוננים מפורשות (`Number(null)===0` היה מזייף את הממוצע).
  - עמודה שמערבבת ציונים ישנים (1–10) עם חדשים (1–100) מסומנת ב-⚠️ עם הסבר ב-tooltip — הממוצע שם חסר משמעות.
  - עיצוב: `table.tbl tfoot td` ב-`css/main.css`.

## שינוי 2026-08-21 — מבנה מעקב הקריאה של הרבנית חרלפ
המפרט המלא + הפערים שנותרו פתוחים: **`READING_ASSESS_SPEC.md`** (ההיררכיה שוחזרה מ-HTML של המייל).
מיגרציה: `supabase/migration_reading_tree_charlap.sql` (**רצה ב-DB ב-21/08**, idempotent).

- **`reading_categories` היא עץ:** `parent_id` + `band` (`low`=א׳–ב׳, `high`=ג׳+, `all`=מורשת).
  3 רמות: כותרת ראשית ← כותרת משנה ← פריט. **צומת בלי ילדים = פריט מדיד** (כך "הכתבה" עובדת).
  נזרעו 26 צמתים ל-low (17 פריטים) ו-38 ל-high (25 פריטים).
- **`classes.reading_band`** קובע איזה עץ מוצג לכיתה (א׳/ב׳ → low, השאר → high). נקבע אוטומטית לפי השם.
- **ערך פריט = `{ok,d,v}`** — תקין/לא תקין + תאריך העברה + ציון 1–100 **אופציונלי** (החלטת יוסף: לצד, לא במקום).
  מספר חשוף ב-`scores` = פורמט ישן וממשיך לעבוד. `{x:1}` = **מחיקה מפורשת** של סימון קיים.
- **`merged()` ממזג את כל ההערכות לפי פריט**, לא רק את האחרונה — חרלפ ביקשה תאריך נפרד לכל פריט,
  ולכן מורה בודק פריטים שונים בתאריכים שונים. ⚠️ אל תחזיר את `forStudent(...)[0]` כמקור המצב.
- **מסך הכיתה:** ברירת מחדל = עמודה לכל כותרת ראשית (`n/m תקין`); צ'קבוקס "תצוגה מפורטת" = עמודה לכל פריט
  עם כותרות מקובצות. "ממוצע כיתתי" = **% תקין** מתוך מה שנבדק + ממוצע ציונים היכן שנרשמו.
- **מלכודות שנתפסו ב-QA ותוקנו** (אל תחזיר אותן):
  - `band='all'` נחשב מתאים לכל שכבה. בעורך זה גרם לעמודות המורשת להופיע בשתי השכבות, ושמירה
    הייתה כותבת להן `band='low'`. העורך חייב **התאמה מדויקת** של band.
  - `buildTree` מסנן `active!==false`; העורך חייב `includeInactive=true`, אחרת אי אפשר להחזיר פריט מושבת.
  - ביטול סימון בלי `{x:1}` לא עשה כלום — הערך הישן המשיך לגבור במיזוג.

## 🚨 מלכודת 2026-08-23 — מודול חדש לא מגיע למשתמשים קיימים
עמנואל דיווח: "רמי ניסה ולא עובד לו, בכלל לא מופיע לו התלמידים".
**השורש:** `readassess` נוסף ל-`MODULES` ב-17/08, אבל מערך `profiles.perms` של כל משתמש נקבע
לפני כן. `setUser` מעדיף `u.perms` על ברירת המחדל של התפקיד — ולכן **אף מורה/מפקח לא ראה את
האריח**, וגם ניווט ישיר ל-`#readassess` הוחזר לדף הבית (`filterByPermissions` מסיר `active`).
רק עמנואל (מנהל, `perms=null`) הצליח.

**כלל: כל מודול חדש ב-`MODULES` מחייב שלושה דברים, לא אחד:**
1. הוספה ל-`roleCaps` (`entry` / `nonMoney`) ב-`js/auth.js`.
2. **מיגרציה** שמוסיפה אותו ל-`perms` של המשתמשים הקיימים — `supabase/migration_perms_readassess.sql`.
3. בדיקה חיה **עם משתמש שאינו מנהל**. QA כמנהל בלבד לעולם לא יתפוס את זה.

**הגנה שנוספה:** מסך "הגדרות והרשאות" מתריע על **"מסכים יתומים"** — מודול שאף איש צוות פעיל
לא רואה (`drawOrphanModules` ב-`js/admin.js`). זה יצוף למנהל במקום להתגלות בתלונה.

⚠️ **הבחנה חשובה:** מפקח הוא `mode:'readonly'` לפי תפקיד — הוא רואה את המסך אך `.row-act`
מוסתר ב-CSS, כלומר **לא יכול להזין**. זו התנהגות מתוכננת, לא באג. כדי שיזין — "רמת גישה" = "גישה מלאה"
בעריכת המשתמש. אל תשנה תפקיד/רמת גישה של משתמש אמיתי בלי אישור מפורש של עמנואל.

## 🚨 מלכודת 2026-08-23 (ב) — "צפייה בלבד" נראה כמו תקלה במערכת
עמנואל: "רמי צריך להזין מבחנים — לא מופיעים שם בכלל השמות".
**השורש:** תפקיד **מפקח** הוא `mode:'readonly'` ב-`roleCaps`. ה-CSS נותן
`body.mode-readonly .qr-card input { pointer-events:none }` — ולכן **בורר התלמידים
במסך מבחנים לא נפתח כלל**, ונראה כאילו אין תלמידים במערכת. זו לא הייתה תקלה אלא חסימת הרשאה.

- **תוקן ב-UI:** כרטיס ההזנה במחולל `makeRecord` (`js/tracking.js`) קיבל `class="entry-card"`,
  ולידו `div.entry-note`. ב-`mode-readonly` הכרטיס מוסתר לגמרי ובמקומו הודעה מפורשת.
  **כלל: אל תשאיר טופס על המסך אם ה-CSS הופך אותו למת — זה מדווח כבאג, תמיד.**
- **רמי:** `access_mode='full'` (לבקשת עמנואל, 23/08) — נשאר בתפקיד מפקח אך יכול להזין.
  ⚠️ זהו override פר-משתמש; לשנות רק באישור מפורש של עמנואל.
- **לקח ל-QA:** מסך שנראה תקין למנהל יכול להיות מת לחלוטין למשתמש אחר.
  **בכל בדיקה של מסך הזנה — בדוק גם עם משתמש `readonly` וגם עם `writeonly`, לא רק כמנהל.**

## 🔐 סיסמאות הצוות גלויות למנהל (החלטת יוסף, 23/08/2026)
`supabase/migration_staff_passwords.sql` (רץ ב-DB). **זו הורדת אבטחה מודעת, לא באג — אל "תתקן" אותה.**
Supabase שומר רק hash חד-כיווני, ולכן העותק הקריא נשמר בנפרד בטבלה `public.staff_passwords`.

- **טבלה נפרדת בכוונה**, לא עמודה ב-`profiles`: ל-`profiles` יש `prof_self_read`
  (`id = auth.uid() OR is_admin()`), כך שעמודה שם הייתה נקראת ע"י כל משתמש על עצמו.
- RLS: `sp_admin_all` = `is_admin()` לכל הפעולות, `force row level security`, בלי GRANT ל-anon.
  **אומת חי:** מפקח מקבל `[]`, anon מקבל `permission denied`, כתיבה נחסמת, וה-RPC מחזיר `not authorized`.
- `admin_set_password` מעדכן hash + עותק קריא. `admin_record_password` לרישום אחרי `signUp`.
- **מילוי ראשוני:** רק מי שעדיין על ברירת המחדל, שזוהה ע"י `crypt(phone, hash) = hash` —
  **בדיקת התאמה, לא פענוח.** סיסמאות שנקבעו קודם אבודות לתמיד ומוצגות "לא ידועה".
- UI: מסך ההרשאות, עמודת "סיסמה" — מוסתרת כברירת מחדל, כפתור "הצג סיסמאות", ותגית
  "ברירת מחדל" כשהסיסמה = הטלפון.

⚠️ **ממצא פתוח:** ב-23/08 **9 מתוך 14** אנשי צוות פעילים היו על סיסמה = מספר הטלפון שלהם.
טלפון של מלמד אינו סוד, ושם המשתמש הוא אותו מספר. הוצג ליוסף; טרם שונה.


## שינוי 2026-08-25 — "מי רשם את זה" + מי רואה מה (בקשת עמנואל במייל)
עמנואל: (1) *"מלמדים בצהריים מעדכנים על תלמיד... אני מתבלבל אם זה אצל המחנך בבוקר או בצהריים —
שאוכל ללחוץ ולראות מי כתב את זה"*; (2) *"מי שלא מוגדר כמחנך — יראה רק את הדיווחים שהוא כתב;
למחנכים — כל מה שעל הכיתה שלהם."*
מיגרציה: **`supabase/migration_author_and_own_reports.sql`** (רצה ב-DB ב-25/08, idempotent).

### מי רשם — `js/author.js` (`window.Author`)
- `created_by` **תמיד היה** בכל טבלאות הדיווח ומתמלא ב-DB (`default auth.uid()`), אבל אף מסך לא הציג אותו.
- החסם האמיתי: `prof_self_read` על `profiles` = `id = auth.uid() OR is_admin()` — מי שאינו מנהל
  לא יכול לתרגם מזהה לשם. לכן נוצרה **תצוגה** `public.staff_directory` (id/name/role/active בלבד,
  `security_invoker=false`, `where is_staff()`, grant ל-authenticated). **אל תוסיף policy קריאה על
  `profiles` עצמה** — יש שם `tz`(ת"ז)/`email`/`perms`.
- API: `load()` (חובה `await` לפני הציור, אחרת הכל "לא ידוע"), `chip/cell/line/name/role/text`.
- מוצג ב: ציר הזמן של מעקב תלמידים (+חלון "פרטי הדיווח" בלחיצה על שורה), רשימות `makeRecord`
  (מבחנים/תפקוד/שיחות/אסיפות/רפואי) + חלון "פרטי הרישום", עמודת **"סומן ע"י"** בנוכחות,
  כרטיס התלמיד, "הועבר ע"י" במעקב הקריאה, וייצוא ה-CSV. בנוסף: קיבוץ **"לפי מי שרשם"** במסך המעקב.
- ⚠️ רשומה בלי `created_by` מוצגת "לא ידוע" — **אל תמלא בדיעבד בשם מנוחש.** (ב-25/08: 0 רשומות כאלה.)

### מי רואה מה
- `sees_only_own_reports()` הוגדרה מחדש **לפי תפקיד**: לא מנהל, לא מפקח, ו(`access_mode='own'`
  **או** לא מחנך). כלומר מלמד/מזכירה/צוות → רק מה שהם רשמו; מחנך → כל כיתתו.
- התבנית הוחלה על **כל** טבלאות הדיווח (קודם רק `attendance`+`tests` הכילו אותה):
  `created_by = auth.uid() OR (can_read_student(student_id) AND NOT sees_only_own_reports())`.
- 🚨 **`INSERT` לא שונה** — כל איש צוות ממשיך לדווח על כל תלמיד. זו ההבחנה מ-
  `migration_report_visibility/scoping*.sql` שאסור להריץ.
- **מצב גישה חדש `own`** (`js/auth.js`): מלמד היה `writeonly`, וה-CSS הסתיר ממנו את **כל** הרשימות —
  הוא הזין דיווח ולא ראה אפילו את שלו. `own` = מזין + רואה את שלו, עם באנר `#modeNote` שמסביר למה
  הוא לא רואה דיווחים של אחרים. קיים גם כאפשרות ידנית ב"רמת גישה" במסך ההרשאות.
  ⚠️ `ownReportsOnly()` בלקוח **חייב להישאר תואם** ל-`sees_only_own_reports()` בשרת.
- **`cls_read` שוחררה ל-`is_staff()`** (הייתה `has_class_access(id)`): מלמד ללא שיוך כיתה קיבל אפס
  כיתות, ולכן כל תלמיד הוצג אצלו "ללא כיתה" ובורר הכיתות בנוכחות היה ריק. שם כיתה אינו סוד
  (`students.class_id` גלוי ממילא). כתיבה נשארה `cls_admin` = מנהל בלבד.

### תיקוני-דרך
- `makeRecord` שמר את השורה המקומית במקום את זו שחזרה מה-DB — רישום חדש הוצג "לא ידוע" עד רענון.
- כרטיס התלמיד קרא `t.date`/`f.date` שלא קיימים (`tests.test_date`, `functioning.report_date`) —
  התאריך היה ריק. תוקן.
- `UI.modal` תומך ב-`cancelLabel` (חלון פרטים נסגר ב"סגירה", לא ב"ביטול").
- **עריכת תלמיד — "עודכן" שקרי.** ברגע שהמלמד קיבל גישה לרשימת התלמידים, נפתח לו גם כפתור
  "עריכת פרטים" — אבל `stu_upd` דורש `has_class_access(class_id)`, ו-PostgREST מחזיר `ok:true`
  בלי שורות כשה-policy חוסמת. תוקן פעמיים: `canEditStudent()` ב-`js/students.js` מסתיר את
  כפתורי העריכה למי שהתלמיד אינו בכיתות שלו, **וגם** השמירה נכשלת במפורש כשלא חזרה שורה.
  ⚠️ `canEditStudent` נשען על `Auth.scopeClasses()` (=`user_class_access`) ואינו מכסה את הענף
  `classes.melamed = auth.uid()` שב-`has_class_access`. כרגע `classes.melamed` הוא `null` בכל
  הכיתות ולכן אין פער — **אם מתחילים להשתמש בעמודה הזו, צריך להוסיף אותה ל-`A.scope`.**

## שינוי 2026-08-26 — לוח ימי הולדת לכל הצוות + סינון "מי עדכן" (בקשת עמנואל במייל)
עמנואל: (1) *"שהלוח של ימי הולדת יהא חשוף לכל הצוות (בלי המסביב של הדשבורד דוחות)"*;
(2) *"בחיפוש מעקב של המנהל — חלונית לחיפוש עדכונים לפי המעדכן, שאוכל לראות כמה כל אחד מעדכן ואיזה דברים."*
**אין מיגרציה ואין שינוי הרשאות** — שני השינויים הם לקוח בלבד.

### לוח ימי ההולדת במסך הבית
- `renderBdayCard(host)` נחשף מ-`js/dashboard.js`; `mountHomeBday()` ב-`js/app.js` מזריק מכל
  `#homeBday` מיד אחרי `.home-hero` וקורא לו מתוך `updateHomeMode()` — כך זה מגיע **גם** לבית
  המקוצר של המורים (`renderTeacherHome`), שאין בו אריחים בכלל. הכרטיס נשאר גם בדשבורד הדוחות.
- ⚠️ **המכל נוצר מ-JS ולא ב-`index.html` בכוונה** — בית-המורה ובית-האריחים חולקים את אותו
  `#page-home`, ומכל אחד מספיק לשניהם.
- `#bdayCard` הפך ל-`.bd-card` (class): הכרטיס מוצג עכשיו בשני מקומות בו-זמנית, ו-id כפול
  ב-DOM הוא באג ממתין.
- **הוסר** `body.mode-writeonly #bdayCard{display:none}` — "חשוף לכל הצוות" כולל מצב הזנה-בלבד.
- `mountHomeBday` יוצא מוקדם בלי `currentUser` ומרוקן את המכל — אחרת נשלפים תלמידים לפני התחברות.
- **אין כאן חשיפת נתונים חדשה:** שמות התלמידים ותאריכי הלידה גלויים לכל הצוות מאז 17/08;
  רק *דיווחים* מוגבלים לכיתה. לא נגענו ב-RLS.
- **שיפורי פענוח תאריך עברי** (יותר תלמידים נספרים במקום ליפול לאזהרת "בלי תאריך תקין"):
  `normMonth` מקלף תחילית ב׳ ("י״ג **ב**אלול תשפ״ו" — זה בדיוק הפורמט שהמערכת עצמה מציגה
  ולכן זה מה שמודבק חזרה לשדה), ו-`parseHebBirth` מקבל יום בספרות ("23 אלול") ולא רק בגימטריה.

### סינון לפי מעדכן במסך המעקב (`js/behavior.js`)
- בורר `#fAuthor` בסרגל הכלים: "כל המעדכנים" + שורה לכל מי שרשם, עם **מונה** — זה ה"כמה כל אחד מעדכן".
- הרשימה נבנית **מהדיווחים עצמם** ולא מרשימת הצוות, כדי שלא יופיעו שמות בלי אף דיווח.
- הסינון יושב ב-`filtered()`, ולכן חל גם על שורת הספירה וגם על **ייצוא ה-CSV**.
- מלכודות שנתפסו ב-QA:
  - `created_by` ריק **ומזהה יתום** נתנו שתי שורות זהות בשם "לא ידוע" שאי אפשר להבדיל ביניהן —
    `auKeyOf` ממזג הכל לדלי אחד (`__none__`).
  - "לא ידוע" אינו אדם ולכן ממוין **אחרון** תמיד, גם כשיש לו הכי הרבה רשומות.
  - אחרי רישום/מחיקה המונים שיקרו — `refreshAuthorOpts()` בונה מחדש ושומר על הבחירה.
  - `grid-template-columns` קשיח ב-inline גובר על ה-media query של 640px; עם פקד חמישי זה
    נמעך בטלפון. הוחלף ב-`repeat(auto-fit,minmax(160px,1fr))`.

### מלכודת שנתפסה אגב כך — בורר התלמיד היה 400px גובה בטלפון
`.stu-pick>*{flex:1 1 auto}` בתוך ה-media query של 600px **לא ניצח מעולם**: הסלקטור הזה הוא
(0,1,1) ואילו `.stu-pick .pk-class{flex:0 1 150px}` הוא (0,2,0). ב-`flex-direction:column`
ה-basis נמדד כ**גובה**, ולכן בטלפון בורר הכיתה תפס 150px ובורר התלמיד 240px — חור ריק של
~300px מתחת לשדות, בכל מסך שיש בו בורר תלמיד. תוקן בכלל נגדי באותה ספציפיות (נמדד חי: 52+50).

## מסך "יצוא והדפסה" + הדפסת מדבקות (2026-08-27)
הועבר ממכינת בית התלמוד (`C:\projects\beit-hatalmud`) לבקשת עמנואל, שביקש את המדבקות.
**שני הפרויקטים מריצים את אותם קבצים** — `js/exporter.js`, `js/labels.js`, `js/pdf.js`,
`vendor/xlsx-lite.js`. תיקון באחד צריך להיות מועתק לשני; אל תפצל אותם.

- **המסך:** בונה גיליון A4 אמיתי וניתן לעריכה (`contenteditable`) מכל מקור נתונים —
  תלמידים, נוכחות, מבחנים, מעקב קריאה, וטבלה ריקה למילוי ביד. `body.printing-sheet`
  מדפיס את הגיליון בלבד.
- **המדבקות:** מלבנים במידות מ״מ במיקום מוחלט + `@page{margin:0}` — **חובה להדפיס
  ב-100% ולא "התאם לדף"**. הפירוט המלא והמלכודות ב-`CLAUDE.md` של בית התלמוד.
- **מה שונה כאן:** אין `cv3Passport` ואין `cv3StudentDocs`, ולכן שני המקורות האלה
  מסוננים אוטומטית מהרשימה (`SOURCES[k].needs`). טבלת הצוות היא `staff_directory`
  (שם בשדה אחד) ולא `staff`. `ui.js` קיבל את `gematria/hebDate/hebYear` שלא היו כאן.
- ⚠️ **מלכודת שנשרפה:** כללי ההדפסה הסתירו את הכפתור הצף לפי **id** (`#helpFab`).
  כאן יש כפתור צף שני — `#aiFab` של המסייע החכם — והוא הודפס על גבי הגיליון.
  הכללים עברו ל-class (`.help-fab`, `.ai-fab`), כך שגם כפתור צף עתידי לא יודפס.
- **QA:** `_qa_exporter.mjs` (26) ו-`_qa_labels.mjs` (45), מול `_demo` על
  `python -m http.server 8798`. `_demo/` נבנה מהקוד עם `config.js` ריק (מצב DEMO)
  ו-`auto.js` להתחברות אוטומטית; שניהם ב-gitignore.
